# Recorded smoke-run evidence

Green-run artifacts are committed here as the evidence trail behind any `verified` support tier —
see `../README.md` for the artifact schema and the graduation rules.

Layout: `results/<hostId>/<YYYY-MM-DD>-<clientVersion>.json`.

There are currently **no recorded runs**: no host is verified. Artifacts must never contain tokens,
connection URLs/fragments, authorization URLs, prompts, or outputs.
