/** Generated active registry. Empty until the complete RBTS v3 bundle is freshly deployed. */
export const tokenlessDeploymentSchema =
  "rateloop-tokenless-deployment-v3" as const;
export const tokenlessDeployedContracts = {} as const;
