# Legacy Self.xyz Faucet

This folder preserves the Curyo v1 required Self.xyz faucet, deployment script,
and focused tests for reference while RateLoop moves to an open rater model.

Files in this folder are intentionally outside Foundry's active `contracts`,
`script`, and `test` roots. The fresh protocol must compile without requiring
the optional proof-of-personhood dependency.
