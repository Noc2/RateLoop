{
  // Gambit mutation run for the FeedbackBonusEscrow conservation proofs.
  // See certora/mutation/README.md and frontend-registry.mutation.conf for how
  // surviving mutants are interpreted (weak-spec signal).
  //
  // Run from packages/foundry (needs CERTORAKEY):
  //   certoraMutate certora/mutation/feedback-bonus-escrow.mutation.conf
  "prover_conf": "certora/confs/feedback-bonus-escrow.conf",
  "mutations": {
    "gambit": [
      {
        "filename": "contracts/FeedbackBonusEscrow.sol"
      }
    ]
  },
  "msg": "FeedbackBonusEscrow spec-strength mutation run"
}
