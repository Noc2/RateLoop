{
  // Gambit mutation run for the pure math libraries (Math.spec). Mutating RewardMath
  // exercises the conservation/bounds/monotonicity rules and the calculateVoterReward
  // path that the mul-div lemma underpins. via_ir-free and fast.
  //
  // Run from packages/foundry (needs CERTORAKEY):
  //   certoraMutate certora/mutation/reward-math.mutation.conf
  "prover_conf": "certora/confs/math.conf",
  "mutations": {
    "gambit": [
      {
        "filename": "contracts/libraries/RewardMath.sol"
      }
    ]
  },
  "msg": "RewardMath spec-strength mutation run"
}
