{
  // Phase 3 (distributor slice): RoundRewardDistributor claim-flag integrity.
  // Inherits compiler/prover settings from base.conf.
  // Run with: certoraRun certora/confs/round-reward-distributor.conf
  "override_base_config": "certora/confs/base.conf",
  "files": ["contracts/RoundRewardDistributor.sol"],
  "verify": "RoundRewardDistributor:certora/specs/RoundRewardDistributor.spec",
  "msg": "RateLoop Phase 3 RoundRewardDistributor"
}
