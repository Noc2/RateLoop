{
  // Phase 10 (Track E): ProtocolConfig address-book role gates. Inherits compiler/prover
  // settings from base.conf.
  // Run with: certoraRun certora/confs/protocol-config.conf
  "override_base_config": "certora/confs/base.conf",
  "files": ["contracts/ProtocolConfig.sol"],
  "verify": "ProtocolConfig:certora/specs/ProtocolConfig.spec",
  "msg": "RateLoop Phase 10 ProtocolConfig role gates"
}
