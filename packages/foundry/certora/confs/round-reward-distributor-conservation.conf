{
  // Track C (aggregate solvency, slice 1): RoundRewardDistributor per-round claimed-amount
  // monotonicity (no clawback / no underflow). Inherits compiler/prover settings from
  // base.conf. Run with: certoraRun certora/confs/round-reward-distributor-conservation.conf
  "override_base_config": "certora/confs/base.conf",
  "files": ["contracts/RoundRewardDistributor.sol"],
  "verify": "RoundRewardDistributor:certora/specs/RoundRewardDistributorConservation.spec",
  "msg": "RateLoop Track C RoundRewardDistributor conservation (monotonicity slice)"
}
