{
  // Track B/C primitive: the nonlinear (a*b)/c <= a bound. Inherits compiler/prover
  // settings from base.conf; overrides via_ir off (pure-arithmetic, no IR codegen
  // needed, mirrors math.conf) and enables the nonlinear-arithmetic SMT backend so
  // the multiply-then-divide inequality is dischargeable (see MulDivLemma.spec).
  "override_base_config": "certora/confs/base.conf",
  "solc_via_ir": false,
  "files": ["certora/harnesses/MathHarness.sol"],
  "verify": "MathHarness:certora/specs/MulDivLemma.spec",
  "prover_args": ["-smt_useNIA true"],
  "msg": "RateLoop mul-div conservation lemma (Track B/C primitive)"
}
