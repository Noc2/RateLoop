{
  // Phase 2: ClusterPayoutOracle. Inherits compiler/prover settings (incl. via_ir
  // + the 0.8.34 yul step override) from base.conf.
  // Run with: certoraRun certora/confs/cluster-payout-oracle.conf
  "override_base_config": "certora/confs/base.conf",
  "files": ["contracts/ClusterPayoutOracle.sol"],
  "verify": "ClusterPayoutOracle:certora/specs/ClusterPayoutOracle.spec",
  "msg": "RateLoop Phase 2 ClusterPayoutOracle"
}
