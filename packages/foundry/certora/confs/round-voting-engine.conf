{
  // Phase 3: RoundVotingEngine.transferReward accounting/auth. Inherits
  // compiler/prover settings (incl. via_ir + the 0.8.34 yul step override) from
  // base.conf. Run with: certoraRun certora/confs/round-voting-engine.conf
  "override_base_config": "certora/confs/base.conf",
  "files": ["certora/harnesses/RoundVotingEngineHarness.sol"],
  "verify": "RoundVotingEngineHarness:certora/specs/RoundVotingEngine.spec",
  "msg": "RateLoop Phase 3 RoundVotingEngine transferReward"
}
