{
  // Phase 6: FrontendRegistry stake conservation (no overstaking, single-use stake
  // return, bounded slash). Inherits compiler/prover settings from base.conf.
  // Run with: certoraRun certora/confs/frontend-registry.conf
  "override_base_config": "certora/confs/base.conf",
  "files": ["certora/harnesses/FrontendRegistryHarness.sol"],
  "verify": "FrontendRegistryHarness:certora/specs/FrontendRegistry.spec",
  "msg": "RateLoop Phase 6 FrontendRegistry stake conservation"
}
