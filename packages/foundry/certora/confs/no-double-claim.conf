{
  // Phase 3 cross-contract: no-double-claim across engine + distributor.
  // Inherits compiler/prover settings from base.conf.
  // Run with: certoraRun certora/confs/no-double-claim.conf
  "override_base_config": "certora/confs/base.conf",
  "files": ["contracts/RoundRewardDistributor.sol"],
  "verify": "RoundRewardDistributor:certora/specs/NoDoubleClaim.spec",
  "msg": "RateLoop Phase 3 no-double-claim across engine and distributor"
}
