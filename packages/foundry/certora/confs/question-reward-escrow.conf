{
  // Phase 4: QuestionRewardPoolEscrow refunded-pool claim gate. Inherits
  // compiler/prover settings from base.conf.
  // Run with: certoraRun certora/confs/question-reward-escrow.conf
  "override_base_config": "certora/confs/base.conf",
  "files": ["certora/harnesses/QuestionRewardPoolEscrowHarness.sol"],
  "verify": "QuestionRewardPoolEscrowHarness:certora/specs/QuestionRewardPoolEscrow.spec",
  // This contract is large (1,490 lines + 11 libraries); the single revert-gate rule does
  // not need the sanity meta-checks, and disabling them keeps the SMT load down.
  "rule_sanity": "none",
  "msg": "RateLoop Phase 4 QuestionRewardPoolEscrow refunded-pool claim gate"
}
