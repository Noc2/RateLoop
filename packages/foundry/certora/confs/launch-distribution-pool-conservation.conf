{
  // Phase 5c (Track C): LaunchDistributionPool aggregate conservation (ghost+hook
  // sum-of-mapping). Inherits compiler/prover settings from base.conf.
  // Run with: certoraRun certora/confs/launch-distribution-pool-conservation.conf
  "override_base_config": "certora/confs/base.conf",
  "files": ["contracts/LaunchDistributionPool.sol"],
  "verify": "LaunchDistributionPool:certora/specs/LaunchDistributionPoolConservation.spec",
  "msg": "RateLoop Phase 5c LaunchDistributionPool aggregate conservation"
}
