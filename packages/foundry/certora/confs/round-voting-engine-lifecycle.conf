{
  // Phase 3b: RoundVotingEngine lifecycle monotonicity + single-use refunds.
  // Inherits compiler/prover settings (incl. via_ir + the 0.8.34 yul step override)
  // from base.conf. Run with: certoraRun certora/confs/round-voting-engine-lifecycle.conf
  "override_base_config": "certora/confs/base.conf",
  "files": ["certora/harnesses/RoundVotingEngineHarness.sol"],
  "verify": "RoundVotingEngineHarness:certora/specs/RoundVotingEngineLifecycle.spec",
  "msg": "RateLoop Phase 3b RoundVotingEngine lifecycle and refunds"
}
