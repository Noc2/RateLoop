# Template for foundry environment variables.

# For local development, copy this file, rename it to .env, and fill in the values.

# We provide default values so developers can start prototyping out of the box,
# but we recommend getting your own API Keys for Production Apps.

# Live-network deploys use a Foundry keystore selected via:
#   yarn deploy --network <network> --keystore <name>

# Alchemy rpc URL is used while deploying the contracts to some testnets/mainnets, checkout `foundry.toml` for its use.
# Get your free API key at https://www.alchemy.com/
ALCHEMY_API_KEY=
# Optional: override Base RPCs for deploys.
BASE_RPC_URL=
# World ID v3 app/action used by RaterRegistry deploys.
NEXT_PUBLIC_WORLD_ID_APP_ID=
NEXT_PUBLIC_WORLD_ID_CREDENTIAL_ACTION=rateloop-human-credential-v1
# Optional explicit World ID v3 router override. Live-network overrides must already
# be deployed on the selected chain; localhost deploys use MockWorldIDRouter.
# WORLD_ID_ROUTER_ADDRESS=
# Optional explicit legacy external nullifier hash override. Leave unset to derive
# from NEXT_PUBLIC_WORLD_ID_APP_ID and NEXT_PUBLIC_WORLD_ID_CREDENTIAL_ACTION.
# WORLD_ID_EXTERNAL_NULLIFIER_HASH=
# RATELOOP_DEPLOYMENT_PROFILE=
# Governed Base production World ID v4 backend issuer rollout. RP ID and action
# are the numeric on-chain World ID v4 domain values, not app_/rp_ strings.
# Keep WORLD_ID_V4_ACTIVATION_CAP at or below the script-enforced 10,000 maximum.
WORLD_ID_V4_BACKEND_SIGNER=
WORLD_ID_V4_RP_ID=
WORLD_ID_V4_ACTION=
WORLD_ID_V4_MAX_CREDENTIAL_TTL=604800
WORLD_ID_V4_RATER_REGISTRY_IMPLEMENTATION=
WORLD_ID_V4_BACKEND_ISSUER=
WORLD_ID_V4_ACTIVATION_CAP=
WORLD_ID_V4_PROPOSER=
WORLD_ID_V4_PROPOSAL_DESCRIPTION=Activate World ID v4 backend issuer
# Etherscan API key is used only for Etherscan-compatible verification flows.
# Get your free API key at https://etherscan.io/myapikey
ETHERSCAN_API_KEY=
# Basescan API key is used for Base verification.
BASESCAN_API_KEY=
# Localhost deploys use the standard Anvil private key directly and do not need a keystore password.
