# Promo video audio assets

- `vo-*.m4a` — placeholder voiceover generated locally with macOS TTS (Samantha).
  Swap for ElevenLabs renders of the same per-beat script (see
  `docs/promo-video-v2-plan-2026-06.md`) without code changes; keep filenames.
- `music.m4a` — original ambient pad bed synthesized by a local script
  (additive sines, no samples), so rights are clean. Treated as a placeholder
  until a licensed track is chosen.
