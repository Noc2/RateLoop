# Promo video audio assets

- `vo-*.m4a` - per-beat voiceover generated by
  `scripts/generate-voiceover.zsh` with the macOS `Reed (English (US))`
  voice. The script keeps the spoken copy conversational and spells phrases
  like `U.S.D.C.` so the TTS reads them cleanly.
- `music.m4a` - original music bed synthesized locally, with no samples, so
  rights are clean.
