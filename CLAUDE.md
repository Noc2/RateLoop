# Claude Workflow Notes

Claude should use [`AGENTS.md`](AGENTS.md) as the source of truth for repository
workflow, production deployment, governance rotation, image handoff, and audit
trust-model guidance.

RateLoop smart contracts are already deployed in production on Base mainnet.
Do not change production contract behavior unless the change is going through the
appropriate governance/admin process, or is needed for an emergency or significant
contract-level issue where off-chain fixes are insufficient.
