name: Legacy World Chain Sepolia Readiness (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Retired Base-first rollout notice
        run: |
          echo "World Chain Sepolia readiness is retired for the fresh Base deployment."
          echo "Use Base Sepolia readiness for staging validation and Base mainnet readiness for production changes."
