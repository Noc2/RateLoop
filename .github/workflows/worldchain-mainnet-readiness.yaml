name: Legacy World Chain Mainnet Readiness (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Retired Base-first rollout notice
        run: |
          echo "World Chain mainnet readiness is retired for the Base-first rollout."
          echo "Use Base Sepolia readiness first; promote Base mainnet intentionally after verification."
